// Start by creating a variable `message`

let name= " Nadia ";
let nameLength = name.length;

const message= `hello my name is ${name} and it is ${nameLength} characters long`;

console.log(message);
